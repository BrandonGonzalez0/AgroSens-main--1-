# ===========================================
# CONFIGURACIÓN DE SEGURIDAD - AGROSENS
# ===========================================
# IMPORTANTE: Copia este archivo como .env y configura tus valores reales
# NUNCA compartas tus secretos en producción

# ===========================================
# SEGURIDAD CRÍTICA
# ===========================================

# Secreto para sesiones (OBLIGATORIO en producción)
# Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=CHANGE_ME_TO_RANDOM_64_BYTE_HEX

# Secreto para tokens CSRF (OBLIGATORIO en producción)
# Genera uno seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CSRF_SECRET=CHANGE_ME_TO_RANDOM_32_BYTE_HEX

# ===========================================
# BASE DE DATOS
# ===========================================

# MongoDB Connection URI
# Desarrollo local: mongodb://localhost:27017/agrosens
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/agrosens?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/agrosens
MONGODB_URI=mongodb://localhost:27017/agrosens

# ===========================================
# URLs Y DOMINIOS
# ===========================================

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:3000

# URL del backend (para desarrollo)
VITE_API_URL=http://localhost:5000

# Dominio de producción (si aplica)
# FRONTEND_URL=https://agrosens.cl
# VITE_API_URL=https://api.agrosens.cl

# ===========================================
# SEGURIDAD ADICIONAL
# ===========================================

# IPs permitidas para endpoints admin (separadas por comas)
# Ejemplo: 192.168.1.100,10.0.0.50
ADMIN_IPS=

# Claves de API válidas (separadas por comas)
# Solo si implementas autenticación por API key
VALID_API_KEYS=

# ===========================================
# SERVICIOS EXTERNOS (OPCIONAL)
# ===========================================

# Weather API keys (si decides usar APIs externas)
# OPENWEATHER_API_KEY=your_key_here
# WEATHERAPI_KEY=your_key_here

# ===========================================
# ENTORNO
# ===========================================

# Entorno de ejecución (development|production)
NODE_ENV=development

# Puerto del servidor backend
PORT=5000

# ===========================================
# CONFIGURACIÓN ADICIONAL
# ===========================================

# Habilitar logs de seguridad
ENABLE_SECURITY_LOGS=true

# Límite de carga de archivos (en MB)
MAX_UPLOAD_SIZE_MB=5

# Timeout para solicitudes (en ms)
REQUEST_TIMEOUT_MS=30000

# ===========================================
# NOTAS DE SEGURIDAD
# ===========================================
# 1. NUNCA commites el archivo .env a Git
# 2. Usa secretos únicos y aleatorios para cada entorno
# 3. Rota los secretos regularmente (cada 90 días)
# 4. Usa variables de entorno en producción (Railway, Heroku, Vercel)
# 5. Mantén este archivo actualizado cuando agregues nuevas variables
