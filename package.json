{
  "name": "agrosens",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run start\"",
    "build": "chmod +x frontend/node_modules/.bin/vite && cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "dev:all": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run start\"",
    "postinstall": "cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "next": "^12.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "vite": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "vite-plugin-react": "^4.0.1"
  }
}
